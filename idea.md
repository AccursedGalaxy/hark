# Claude Code Task management

Instead of doing everything headless with "-p" -> we just use it as a general notification/control center.

and we get and send information to claude code sessions living in tmux panes
