# 2026-05-29

- 17:24 — Claude Code synchronous decision hooks (PreToolUse / UserPromptSubmit) differ from fire-and-forget notifications: they fire for EVERY session globally, the hook command must preserve stdout (use `curl ... 2>/dev/null || true`; server returns JSON with hookSpecificOutput; return `{}` for no opinion, `{"hookSpecificOutput":{"permissionDecision":"deny",...}}` to block; gate server-side on session_id + cwd + tool_name). See `00-inbox/claude-code-decision-hooks.md`.
- 17:24 — Charter-as-stdout pattern: inject a persona/charter into a live agent session by having it run a CLI command whose STDOUT is the charter text — agent reads it as a Bash result and adopts the role. Cleaner than keystroke simulation; uses CLAUDE_CODE_SESSION_ID env to self-identify. See `00-inbox/charter-as-stdout.md`.
- 17:24 — Footgun: `pkill -f dist/server.js` matches both test orphans and prod systemd instances running the same binary. Kill by exact PID or unique PORT env instead.
