# hark — PLAN

*Living state of this project. Hark reads it at every session start;
sessions keep it accurate as they work. Each section opens with its
contract in italics — those lines are binding.*

## North Star

*What this project is for. 2–4 sentences. Slow-changing — only edit when
the vision actually shifts.*

hark is a notification and remote-control hub for Claude Code sessions running on this host. It reads `~/.claude/sessions/*.json` + per-session transcript JSONL and drives sessions via `tmux send-keys`, so any active session is reachable from any device on the tailnet without replacing the TUI. The point is attention triage and full mobile control — coexistence with terminal use, not replacement. On top of that single-session control, hark is growing an **orchestration layer**: running multiple sessions as a coordinated team of role-playing agents (Researcher/Coder/Tester/Documenter/Reviewer), each in an isolated git worktree, driven autonomously via the same tmux path — turning hark into a power tool for managing a whole project's worth of agents.

## Now

*Active threads being shipped right now. Hard cap: 3 items. One bullet
per thread; one line of context allowed beneath it.*

- **PM-Head harness is LIVE on `main`** (@ `ea0dbe9`, deployed) — `main` is the single canonical trunk; every other branch is a feature/refactor branch. The whole stack is here: orchestration layer + head-session model + PM-Head phases A–D + dogfood fixes + the interaction features (pr-body #15, `hark pr --base` #16, status truncation #14/#17, `--task-file` #19) + the base-drift fix (#18: `set-base` verb + fetch-before-branch). Live orch `baseRef` is **`main`** — workers fetch + fork the live `origin/main` tip. Active frontier: dogfood the harness on hark's own backlog — **now UNBLOCKED** (the harness-reliability blocker shipped + deployed + validated live 2026-05-29, see Shipped). Deploy = rebuild + `systemctl --user restart hark.service` (dist + `public/` both rebuild; verified current as of `ea0dbe9`).
- **The Board — PM operational substrate + org keystone (CORE LIVE on `:3000` 2026-05-30; design RESOLVED 2026-05-29).** Core shipped (#27) + activated — `hark board add/list/show/set/link/assign/close` is now the keyed operational SoT (task status/deps/workstream live on the board; PLAN.md = narrative only). Binding design = the **`## RESOLVED`** section of `.hark/board-plan.md` (supersedes this bullet and the doc's earlier Parts where they conflict — read it before executing). The session recursed "build the board regardless" down five layers via one move (*refuse the clean-but-false artifact*) and reached: **the gate has exactly ONE member — adding a second supervisory locus** (cell adjudicates its cheap mechanical form, an admission controller bounding in-flight `k`, vs. its expensive judgment form, a Lead). Everything else is **ungated, five layers each with a named warrant**: core (proven-need: keyed upsert kills string-match edit failures), `depends_on`+lazy-branch, dispatch *schema*, dispatch *behavior* (PM *permitted* to fan out — `permitted ≠ unbounded`; the `k` cap is the gated thing), per-project granularity (NOT per-orch — a per-orch board hides the cross-orch load that fills the cell). Build = `node:sqlite` (reuse Phase-0 metrics layer), `hark board` CLI, native SoT (GH = projection). **Sequencing (corrected): ship CORE now in parallel (proven warrant, gates on nothing); but the FIRST COUNTED saturation epic is hard-gated on FOUR prerequisites — (1) breaker hardening [unattended safety], (2) tool-call-transport instrumentation [the deferred `turns`/`tool_calls` tables + three-state `--for` annotation + intent-from-transcript; doubles as the first IO-track task], (3) dispatch schema live, (4) per-project granularity live.** An epic run before all four is uncounted-or-biased (selection bias toward the messy high-context epics = where the build signal lives). Build/kill cell = exhaustion at the **joint floor** (k→slurp coupled DAG; read admission first); near-certain terminus = **kill-with-tripwire**. Only open Q: backfill-vs-drain.
- **Dogfood the harness on hark's own backlog — NOW THE FRONTIER (unblocked).** Both reliability blockers shipped+deployed (2026-05-29), so the harness is safe to drive unattended: `hark agent stop`, runaway circuit-breaker, honest hide-done status, persisted DONE-markers (`hark agent summary`), and reliable fire-once PM wake-up are all LIVE on `:3000`. Next: pick real product work from Next/Inbox and run it through the PM→worker→PR loop. Validated end-to-end this session: dispatched 5 workers + 1 integrator across the two blockers; the wake-up fix specifically fixes the "PM slept through a finished worker" friction. **ACTIVE DOGFOOD (2026-05-30):** first attempt was `depends_on`-at-handoff (board workstream `depends-on-handoff`) — its coder hit a **breaker FALSE-POSITIVE** (killed at 40 turns / 86k tok doing legit non-repeating reads across 7 files; Trigger-2 `NO_PROGRESS_TURNS=40` can't tell convergent research from a spiral). So `depends-on-handoff` is PARKED and the pivot is now in flight: **board workstream `breaker-judge`** — replace Trigger-2's auto-kill with a Haiku stuck-judge that escalates to the PM instead of killing (design: `.hark/breaker-judge-plan.md`; coder→tester→reviewer chain). **IMPL LANDED** on `hark/pm-hark/coder-7cba42` (attempt 3; 6 files +641/-5, 4 commits, 802 green) — but only after the breaker false-positived TWICE more on its own builder, forcing two human interim relaxations of the live breaker (turns 40→100, ceiling 120→250, token-tally now excludes cache reads; committed `ad8229c`). Now in independent test (tester hand-wired onto the coder branch, since auto-branch-at-handoff = the parked `depends_on` task). Live task status ON THE BOARD (`hark board list`), not here. DOGFOOD, not saturation data.

## Next

*Committed, not started. No hard cap, but if this list exceeds ~7 items
it has become a dumping ground — flag it and force triage back into Now
or out of the doc.*

- **Repo-owned Claude integrations + symlink — DECIDED (B), READY TO BUILD (no worker dispatched yet).** Goal: ship hark's own Claude Code integration in-repo so it updates on `git pull`. Researcher `0b53c3` inventory: only TWO things are hark-specific — (1) the `head` slash-command, already in the repo but **gitignored** (still at `.claude/commands/head.md`, `.claude/` is in `.gitignore`); (2) **12 inline `curl → :3000/api/hook` entries** in `~/.claude/settings.json` (PreToolUse guard / UserPromptSubmit news / Stop autonomy). Everything else is PERSONAL — out of scope. **Decision B (human, 2026-05-29): ship ONLY the `head` command for now; do NOT auto-merge settings.json.** → BUILD: create `integrations/claude/` holding the `head` command (un-gitignore it / relocate), add an idempotent opt-in `install.sh` that **only symlinks** the command into `~/.claude/commands/` (so it auto-updates on pull), and a short README **documenting** the 12 hook entries for manual setup (don't write to the user's settings.json). Dispatch a Coder with an intent-level brief.
- **Validate active autonomy + decide default-on** — exercise `HARK_ORCH_AUTONOMY=1` end-to-end (idle-advance push, blocker→human escalation, self-review termination), populate `costUsd` (still defaults to 0 in `protocol.ts`; needs the `web/src/lib/usage.ts` pricing table shared server-side), then decide whether to default the dial on. The live run validated terminate-on-done + worktree deps + idle-advance routing on `:3000`; the wake-up reliability gap is now tracked under the durable-context blocker in Now.
- **Resolve `dependsOn` at handoff** — *(IN FLIGHT 2026-05-30 — board workstream `depends-on-handoff`; see Now)* lazy-branch a downstream worktree off the upstream's branch HEAD when it hits `[[HARK:HANDOFF]]`/`[[HARK:DONE]]` and has committed, instead of cutting it from `baseRef`. The `dependsOn` field already exists on `OrchAgent` (stored, threaded through `spawnAgent`), but the worktree is still off base — so a depends-on tester sees pristine base, not the coder's diff, and the HANDOFF marker today only flips lifecycle to `review` (moves no code). Unblocks fanning out coder + tester(`--depends-on coder`) together. Keep one worktree per agent — never share a live tree.
- **Spawn echo + tmux window names** — `hark agent spawn` returns only `spawned <role>: <agentId>`; echo branch + base + orch key so a PM can run `hark pr <id>` without a `git`/status lookup. Name agent tmux windows `<project>-<role>` (e.g. `hark-coder`) instead of cryptic ids.
- **UI polish (deferred per "ignore UI for now")** — capture-modal image attach (drag-and-drop + Ctrl+V, mirroring the composer upload flow); passive "modified by another session" dot in `PlanPanel` when `planMtime` advanced; Web Push (service worker + VAPID for closed-app mobile notifications); chat-input typing lag in `Composer.tsx` (laggy only in chat, not the capture form — suspect controlled-value re-render cost); larger + copyable code blocks in session-history output. (Full build record: `docs/pm-head-harness.md`, `docs/orchestration-head.md`.)

## Shipped

*Newest first. Keep the last 10 lines here; move older entries to
`.hark/shipped-archive.md`. Git history is the real archive — this
section exists to answer "what just happened" for a cold start, not
to be complete.*

- **Board core — SHIPPED** (PR #27 on `main`, merged 2026-05-30; chain `coder-e3bff0`→tester `af0dd7`→reviewer `a244b7`(APPROVE v1)→fix `coder-53e509`→focused reviewer `42e4ca`; 9 files +1995/-2, 769 green; activates on redeploy). The keyed task store the PM design called for: `node:sqlite` board (a SEPARATE source-of-truth db, NOT the rebuildable `metrics.db`) + `hark board add/list/show/set/link/assign/close` CLI; `tasks`(status/assignee/workstream/`depends_on`, per-project) + append-only `task_events`. Tester verified upsert idempotency + `task_events` append-integrity under 8-thread concurrency; fix-reviewer verified SF-1 (real forward-migration runner — the SoT durability promise) + SF-2 (`BEGIN IMMEDIATE`-wrapped RMW — concurrency safety the detector needs) CLOSED **by mutation testing**. **Saturation prereqs #3 (dispatch schema) + #4 (per-project granularity) SATISFIED; detector half of #2 now UNBLOCKED.** Nit: the `setTask`-fields concurrency test is weak — the `link` test is the real SF-2 guard. First end-to-end board build = a 5-stage chain, fully dogfooded.
- **Breaker hardening — SHIPPED + DEPLOYED** (PR #26 on `main`, merged + redeployed 2026-05-30; `coder-d64f0b`, reviewer-gated `684225` APPROVED, 3 files +492/-48, 70/70 breaker suite green). Upgrades the original signature-only circuit-breaker (#21) that the live 7.6M-tok VARIED-probe spiral slid past: now also trips on **progress-based** stall (N turns/calls with no new commit AND no diff change — keyed on committed *diff*, not commit count, so a no-op commit can't launder a spiral) PLUS a **hard per-worker ceiling** (~≤120 turns/≤4M tokens bounds every path). The 7.6M varied-probe miss is reproduced by an integration test that NOW trips; reviewer walked the false-negative path and proved it closed. Existing signature trigger kept as one trigger among several. **Saturation prereq #1 (unattended safety) SATISFIED** — the harness is now genuinely safe for an unattended saturation run. (NOTE: Shipped is now 11 entries; trim the oldest `Infra bug #2` line to `.hark/shipped-archive.md` next calm pass.)
- **Transport instrumentation (CAPTURE half) — SHIPPED** (PR #25 on `main`, merged 2026-05-30; `coder-004ff7`, 721 green, 4 commits; activates on redeploy). Schema v2 adds the deferred `turns`+`tool_calls` tables + `turn_ingest_state` cursor to `metricsDb.ts`; pure `captureTurns()` projects the transcript's assistant-side `tool_use` stream into per-turn + per-call records (per-call: stable call-id, **channel**, batch membership/size/position), riding the existing reconcile loop — no extra transcript read, idempotent `OR IGNORE`, rebuildable. Load-bearing invariant GUARDED by a test capturing a tool call with ZERO `tool_result` events present → intent comes from `tool_use`, survives a dropped result, never self-report. Detector + discipline-covariate + three-state pull + `--for` deliberately UNBUILT (gate on merged board core — now unblocked once #27 lands). **Saturation prereq #2 capture-half SATISFIED**; detector-half still pending.
- **Phase 0 central metrics DB — SHIPPED + DEPLOYED** (PR #24 on `main`, merged + redeployed 2026-05-29, 715 green; coder `d800f8`, reviewer-gated `9a9d2c` APPROVED, 9 files +1151/-90). `node:sqlite` `metrics.db` AUGMENTING the JSON store (derived/rebuildable), ingested off the 3s reconcile loop reusing the existing transcript read: `token_samples` time-series, **`costUsd` now computed everywhere** (was always 0), all PR outcomes logged, shared price table (`src/shared/pricing.ts`). First clean coder→reviewer→PR chain. Two non-blocking should-fixes + the spiral/resilience lessons in Inbox. Closes Phase 0 of the dogfood data layer.
- **Durable PM↔worker context channel — SHIPPED + DEPLOYED** (PRs #22 + #23 on `main`, merged + redeployed 2026-05-29, 689 green). (a)+(b) PR #23 (`coder-1ffe0b`, 7 files +563/-28): persists a worker's full terminal marker on the `OrchAgent` record + `hark agent summary <id>` (survives reaping), and a `headWokeAt` fire-once guard shared by the marker AND reconcile paths so the PM wakes exactly once on any terminal transition (fixes the "PM slept through a finished worker" bug). (c) PR #22 (`coder-b76c65`, +104/-0): the diff-inflation gap was a MISDIAGNOSIS — `hark agent diff`/`branchGitSummary` already use three-dot `base...branch` (merge-base); shipped a regression-guard test instead. Residual (local-base-staleness) parked in Inbox.
- **Harness reliability blocker — SHIPPED + DEPLOYED + validated live** (2026-05-29). Gaps 1+3 = PR #20 (`coder-19be8e`, 11 files +306/-10, 660 green): `hark agent stop <id>` (SIGTERM + new terminal `stopped` lifecycle, idempotent + zombie-safe, keeps worktree), `orch status` default-hides terminal workers (`--all` shows), `isTerminalLifecycle` predicate in `protocol.ts` as single source of truth. Gap 2 = PR #21 (circuit-breaker in `controller.ts`; `coder-733cdf` → integrator `coder-41cbda`, +448/-2): trips on N consecutive identical no-op commands → flips worker to `blocked` w/ reason, existing reaper kills it; merged clean atop #20, combined tests green. Both merged + redeployed; validated LIVE — `hark agent stop` cleared the `researcher-0bb21b` zombie (→`stopped`) and hide-done confirmed (`orch status` now clean). Harness is now safe to drive unattended.
- **Base-drift fix — workers now fork the live `main` tip** (PR #18 on `main` @ `7f74829`, deployed 2026-05-29, 641 green; `coder-c61871`, +138/-1). Two parts: `hark orch set-base <ref>` (overwrites `baseRef` in place + appends a `note` audit event, `POST /…/base`, CLI verb/render) re-points a frozen `baseRef` without recreating the run; and `addWorktree` now `git fetch origin <base>` + branches off `origin/<base>` via the pure `resolveWorktreeBase` helper (best-effort fallback to the local ref for HEAD / local-only / no-remote — no regression). `set-base main` applied, so the orchestration is off the stale `pm-head-harness` base for good. Opened via `hark pr --base main` with an auto-generated body — full PM→worker→PR loop through the harness, no raw-`gh` bypass.
- **Interaction-smoothness features — all on `main`** (2026-05-29, dogfooded end-to-end through the PM-head→worker loop): (1) **`hark pr` generates a real PR description** from the worker task + commit log + diffstat — PR #15, `coder-c7bee7`. (2) **`hark pr --base <ref>`** to target an alternate base — PR #16, `coder-2edc05` impl, `coder-4da6cf` integrated. (3) **`hark orch status` task truncation** — pure `truncateTask(s,60)`, one compact line per worker — PR #14→#17, `coder-8f19e5`. (4) **`hark agent spawn/brief --task-file <path|->`** — supply the brief from a file or stdin (heredoc, no temp file); pure `taskFileArg`/`injectTask` + IO in `bin/hark` — PR #19, `coder-37c4f8`. All unit-tested, tsc clean.
- **All landed on `main` @ `bbe2362`** (2026-05-29, 624 green, tsc clean, pushed): the PM-Head harness (orchestration layer + head-session model + phases A–D) + the four dogfood fixes below, fast-forwarded from `pm-head-harness`. `main` is now the clean base with every new feature.
- **Infra bug #3 — `hark pr` handles a local-only base** (dogfound + fixed 2026-05-29). Added a `baseOnOrigin` DI dep (`git ls-remote --heads origin <base>` in `worktree.ts`) checked before `gh pr create`; base-present is unchanged, base-absent returns a structured `no_base` result with a friendly push-then-rerun / merge-locally message instead of the raw gh GraphQL error (no auto-push of the human's WIP). Both paths unit-tested. Worker `coder-604981` (self-terminated at 63 turns — validated fix #1).
- **Pure-PM guard false-positive fixed** (dogfound + fixed 2026-05-29; it had blocked the PM's own `hark agent spawn`). `pmGuard.ts` `splitStatements` is now quote-aware (operators inside quotes don't split), plus a per-statement allowlist for the `hark` dispatch CLI so a `--task` payload is never parsed as tree mutation — while a real mutation chained after still denies. Tests cover spawn-with-rich-prose ALLOW + chained-rm DENY + all prior deny cases. Worker `coder-e48af9` (self-terminated at 33 turns).
- **Infra bug #1 — terminate workers on terminal lifecycle** (dogfound 2026-05-29; the dominant token sink, ~1.3M tok/run of idle spin). The reconcile loop (`reconcileOrchestrations` in `src/server.ts`) now SIGTERMs a worker's session process the first time its lifecycle is `done`/`blocked`/`failed`, via the new `Orchestrator.killTerminalAgent` — which reuses the existing `killSession` dep (same teardown kill, tolerant of a null/dead pid), KEEPS the worktree+branch (the head still reads the work), and uses the new `OrchAgent.killedAt` to fire once, not every 3s tick. Independent of the autonomy dial — a finished worker is always reaped. Regression-tested in `orchestrator.test.ts`.
- **Infra bug #2 — worktrees get `node_modules`** (dogfound 2026-05-29). `addWorktree` now symlinks the repo's installed deps (root + `web/`) into each fresh worktree via the new `linkNodeModules` helper, so workers can build/typecheck/vitest instead of hitting `SKIP_TYPECHECK: no node_modules`. Symlink not copy (instant, zero disk); best-effort + idempotent (skips absent sources / existing targets, swallows fs errors). fs-only tests in `worktree.test.ts`. (617 tests green, tsc clean.)

## Inbox

*Raw captures, timestamped as `- [YYYY-MM-DD HH:MM] text`. The required-pass
section: every line must be gone or tagged before this session ends.*

- *Relevant to current work → incorporate, then remove the line.*
- *Otherwise → promote (to Now / Next), delete (noise), or keep with a
  one-word reason in brackets: `[blocked]`, `[maybe]`, `[waiting-on-X]`.*
- *A bare untagged line surviving past session end is the signal that
  the previous session failed its pass — triage it first.*

- [2026-05-29 PROCESS] **PM context discipline: stop pre-reading the codebase to write line-precise briefs.** The PM has been doing scoping research itself (reading `cli.ts`/`worktree.ts`/`store.ts`/`bin/hark` to cite exact lines) — clean diffs, but it burns the PM's scarce context on work a worker can do in its own isolated context for free. **Default instead:** (1) write *intent-level* briefs — what + constraints + acceptance criteria — and let the coder locate the code itself (it has a full worktree + tests); (2) for unfamiliar areas or uncertain design, spawn a **Researcher** to map it and return a findings brief (its context absorbs the file-reading, not the PM's). Tradeoff: detailed briefs landed workers green first-try, so don't go *too* thin on tricky changes — but most items only need intent + acceptance criteria. [feedback]
- [2026-05-27 18:40] easy issue to implement https://github.com/AccursedGalaxy/hark/issues/13 [maybe]
- [2026-05-27 18:46] typing bubbles for claude code inside the session view [maybe]
- [2026-05-29 17:35] Alt+Enter newline in chat — RE-VERIFY. [maybe-resolved] The fix is in source (`Composer.tsx:406` `e.key==="Enter" && e.altKey`) AND in the live bundle (`public/index.html` rebuilt 19:08 @ `ea0dbe9`), so the original "still SENDS" report's #1 suspect — a stale Vite bundle — is now gone. Needs a fresh hard-refresh + retest on `:3000`. If it STILL sends after that, verify `e.altKey` actually reaches the browser (Hyprland may grab Alt+Enter at the WM layer). (Cmd+Enter = `metaKey`, separate case.)
- [2026-05-29 FOLLOWUP] **Worker-diff vs LOCAL base staleness (from `coder-b76c65`).** The three-dot `base...branch` diff is correct, but the merge-base is taken against the LOCAL base ref. If local `main` lags `origin/main` (the tip workers actually fork from, per the #18 base-drift fix), the merge-base sits behind the real fork point → a worker's diff/diffstat can still show commits it didn't make. Fix would measure against `origin/<base>` (fetch-then-diff), mirroring how `addWorktree` already forks from `origin/<base>`. Narrow; only bites when local base is stale. [maybe]
- [2026-05-29 SHOULD-FIX, reviewer of PR #24 — fast-follow] **Metrics-DB polish (non-blocking; do before Phase 1 copies the pattern).** (1) `server.ts:1576` — `insertPrOutcome` is the ONE ingest write not individually try/caught; it sits in the route's outer try/catch, so a sqlite throw returns HTTP 500 on an ALREADY-successful `hark pr` (gh ran, `created` event already appended) → could prompt a duplicate-PR retry. Wrap it best-effort or move after `res.json`. (2) `server.ts:1944-1949` + `controller.ts:851`/`990` — under autonomy the HEAD transcript is read TWICE per 3s tick (`onHeadSignal` + unconditional `refreshHeadMetrics`, no offset read); have `onHeadSignal` return its sample. Nits: first-ever ingest of a large `events.jsonl` inserts every row in one tick (`metricsDb.ts:298`; consider a batch cap); torn trailing line in `readEventsFromOffset` dropped permanently (`store.ts:514`, acceptable for a rebuildable model); WAL sidecars `metrics.db-wal`/`-shm` (`metricsDb.ts:301`) → dogfood cleanup checklist. [harness-reliability]
- [2026-05-29 FOLLOWUP] **Org-design evolution → RESOLVED into `.hark/board-plan.md` `## RESOLVED`.** The flat-star→layers reasoning (central BOARD + a LEAD "keystone") was superseded this session: the Lead is NOT the keystone — the gate has exactly one member (a second supervisory locus, mechanical-controller-vs-Lead adjudicated by evidence), and the board is five ungated layers. Both earlier open Qs answered there (native SoT; reuse head machinery for the locus). See the Now bullet. [superseded]
- [2026-05-29 BUG/PROVEN] **Breaker must be PROGRESS-based + hard token ceiling — live 7.6M-tok miss.** Phase-0 coder `fde8ed` spiraled to 147 turns / 7.6M tokens with ZERO commits and the circuit-breaker never tripped: its probe commands were VARIED (different greps/echoes/`/tmp` round-trips), so the consecutive-identical-signature heuristic slid past. Root trigger: a flaky-tooling cascade (parallel tool batches where one failure cancels the whole batch incl. git commits → total work loss → re-probe spiral). FIX: trip the breaker on N turns / K tool calls with no new commits AND no diff change (progress-based, signature-independent — the breaker already tracks a progressKey baseline), PLUS a hard per-worker token/turn ceiling. This is the concrete upgrade of the alternation-blind-spot follow-up below. [harness-reliability]
- [2026-05-29 FOLLOWUP] **Worker tooling-resilience defaults in coder role prompt (`roles.ts`).** The flaky tool IO this session (blank/dropped/duplicated output, `Error writing file`, parallel-batch cascade-cancel) wiped a worker's whole build because it batched writes+commits together. Bake into the worker role prompt: run tool calls SEQUENTIALLY (don't bundle into parallel batches — one failure cancels all), commit each logical piece IMMEDIATELY, retry a single failed call rather than re-batching. Makes every worker survive the glitch by default. [harness-reliability]
- [2026-05-29 INVESTIGATE] **Root-cause the IO glitch.** Blank/dropped/duplicated tool results + `Error writing file` + parallel-batch cancellations hit BOTH the PM session AND workers this session → environmental (Claude-session IO or hark's tmux send-keys/output-capture under load), not task-specific. Diagnose whether it's hark-side (correlate with output volume / parallel batches); if so it's the highest-leverage reliability fix. [harness-reliability]
- [2026-05-29 FOLLOWUP] **Circuit-breaker hardening (from Coder B, deferred).** Two known gaps in the shipped breaker, both deliberate trade-offs for low false-positives: (a) *sub-agent root cause* — the breaker catches the symptom, not the trigger; a clean defence-in-depth seam is denying the `Agent`/`Task` tools for workers via the permission classifier at `CLAUDE_COMMAND` (`orchestrator.ts:84`, `claude --permission-mode auto`). (b) *alternation blind spot* — breaker keys on a trailing run of ONE signature, so a worker alternating between two distinct probe types wouldn't trip; extension is "≤2 distinct signatures in last 2N calls" IF alternation is seen live. Neither is a blocker. [maybe]
- [2026-05-29 BUG] **pmGuard false-positive on heredoc stdin.** `hark agent spawn coder --task-file - <<'BRIEF' … BRIEF` was DENIED by the pure-PM guard as "redirection into the project tree" — but `<<` is a stdin heredoc, not a file write. The quote-aware splitter still treats heredoc redirection as a tree mutation. Workaround used: write brief to `.hark/*.md` then `--task-file .hark/x.md`. Fix: teach the guard that `<<`/`<<<` (and stdin `<`) are reads, not writes. Sibling to the [[shipped]] quote-aware splitter fix. [harness-reliability]
- [2026-05-29 21:55] Improve context efficiency across the whole stack once our first dogfood lop and our data collection steps are done.
